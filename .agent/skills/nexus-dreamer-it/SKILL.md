# nexus-dreamer-it

NexusDreamer is an immutable image-revision chain harness. Route work through commands and services, publish outcomes as domain events, compile inference intent through the AST before provider execution, and never mutate an accepted image artifact in place.
